#include "Utilities.h"
#include <math.h>
#include <utility>
namespace utilities
{
    std::pair<double, double> squarify(double x, double y)
    {
        double angle = atan2(x, y);
        
    }

}